{
  "name": "@intentlang/core",
  "version": "1.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "generate:ebnf2antlr_ast": "node ./scripts/ebnf2antlr_ast.mjs -i ./grammar/IntentLang.ebnf -o ./grammar --grammar IntentLang",
    "generate:antlr4ts": "npx antlr4ts-cli -visitor -o ./src/generated ./grammar/IntentLangLexer.g4 ./grammar/IntentLangParser.g4 && node ./scripts/patch-antlr-esm.mjs ./src/generated/",
    "generate:all": "pnpm run generate:ebnf2antlr_ast && pnpm run generate:antlr4ts",
    "build": "pnpm run generate:all && tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json -w",
    "lint": "echo \"(add eslint later)\"",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "antlr4ts": "0.5.0-alpha.4"
  }
}
