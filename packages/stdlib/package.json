{
  "name": "@intentlang/stdlib",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "echo \"(intent build)\"",
    "dev": "echo \"(pnpm intent build --watch)\"",
    "lint": "echo \"(pnpm intent lint)\"",
    "test": "echo \"(pnpm intent test)\"",
    "typecheck": "echo \"(pnpm intent typecheck)\""
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "@vitest/coverage-v8": "^3.2.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
